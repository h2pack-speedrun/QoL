-- Default configuration values.
-- Users can override these via Chalk (config.lua in plugins_data).
return {
    Enabled             = true,
    DebugMode           = false,
    KBMEscape           = true,
    ShowLocation        = true,
    SkipDeathCutscene   = true,
    SkipDialogue        = false,
    SkipRunEndCutscene  = true,
    SpawnLocation       = true,
    VictoryScreen       = true,
}
